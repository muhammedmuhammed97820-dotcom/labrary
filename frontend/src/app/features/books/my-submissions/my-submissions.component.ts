import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { RouterLink } from '@angular/router'; import { BookApiService, Book } from '../../../../core/services/book-api.service';
@Component({selector:'app-my-submissions',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./my-submissions.component.html',styleUrl:'./my-submissions.component.scss'})
export class MySubmissionsComponent { readonly api=inject(BookApiService); books:Book[]=[]; loading=true; ngOnInit(){this.api.mySubmissions().subscribe({next:r=>{this.books=r||[];this.loading=false},error:()=>this.loading=false});} author(b:Book){return typeof b.author==='string'?b.author:b.author?.name||'—';} status(b:Book){return b.status==='approved'?'تمت الموافقة':b.status==='rejected'?'مرفوض':'قيد المراجعة';} }